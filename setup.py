import subprocess

if __name__ == '__main__':
    subprocess.run(['pip install git+https://gitee.com/wd_kind/MobileSAM.git'])
    subprocess.run(['pip install timm'])
    subprocess.run(['pip install git+https://github.com/chouheiwa/nnSAM.git'])
